/**
 * Ripple AWS S3 Configuration
 *
 * Copy this file to config.js and edit to suit your preferences.
 */

{
  "aws": {
    "key": "aws-key",
    "secret": "aws-secret",
    "bucket": "aws-bucket"
  }
}
