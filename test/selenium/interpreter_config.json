{
	"type": "interpreter-config",
	"configurations": [
		{
			"settings": [
				{
					"driverOptions": {
						"host": "localhost",
						"port": 4445
					},
					"browserOptions": {
						"browserName": "googlechrome",
						"username": "${SAUCE_USERNAME}",
						"accessKey": "${SAUCE_ACCESS_KEY}"
					}
				}
			],
			"scripts": [
				"test/selenium/tests/*"
			]
		}
	]
}
